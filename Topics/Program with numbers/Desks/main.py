first = abs(int(input()))
second = abs(int(input()))
treeth = abs(int(input()))
first = (first % 2) + first // 2
second = (second % 2) + second // 2
treeth = (treeth % 2) + treeth // 2

result = first + second + treeth

print(result)